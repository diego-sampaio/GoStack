import React from "react";
import { render } from "react-dom"; /* render: responsável por renderizar um componente do React dentro de um conteúdo do HTML */

import App from "./App";

render(<App />, document.getElementById("app"));
